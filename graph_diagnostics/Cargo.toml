[package]
name = "graph_diagnostics"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = "0.40.1"
storage = { path = "../storage" }
graph = { path = "../graph" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
indexer = { path = "../indexer" }
