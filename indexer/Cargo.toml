[package]
name = "indexer"
version = "0.1.0"
edition = "2024"

[dependencies]


graph = { path = "../graph" }
parser = { path = "../parser" }
query = { path = "../query" }
storage = { path = "../storage" }
ignore="0.4"
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.10.1"
