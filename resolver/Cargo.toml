[package]
name = "resolver"
version = "0.1.0"
edition = "2024"

[dependencies]
storage = { path = "../storage" }
query = { path = "../query" }
graph = { path = "../graph" }
rusqlite = "0.40.1"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0"
